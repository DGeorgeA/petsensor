# Local-only secrets for the ONE-TIME reference-audio upload.
# Copy this file to .env.upload (gitignored), paste your service_role key, then:
#   npm run fixtures:upload
#
# The service_role key bypasses Row Level Security — treat it like a password.
# NEVER commit .env.upload, paste the key into chat, or ship it to the client.
# Dashboard → Project Settings → API → service_role (secret).

SUPABASE_URL=https://tcmcetpfdgpujayjbzrs.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
